document.getElementById("cookieImage").onclick = () => {
    if (Math.random() > 0.2) {
        alert("I like this kitty dance!");
    } else {
        alert("Don't crumble your cookies here!");
    }
}